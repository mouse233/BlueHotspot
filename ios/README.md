# iOS client

This directory contains the native SwiftUI controller skeleton.

The MVP iOS client will scan, pair, authenticate, and send start/stop commands
over BLE. It does not receive or manage the Android hotspot SSID/password.

Create/open the Xcode project on macOS. The source layout is intentionally kept
independent from the generated `.xcodeproj` files.
